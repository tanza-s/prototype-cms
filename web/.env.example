PUBLIC_CMS_URL=http://localhost:3000

# Timezone used to format event dates. Payload stores UTC, and a static build
# would otherwise format in the build machine's local zone.
PUBLIC_EVENT_TIME_ZONE=America/Los_Angeles
